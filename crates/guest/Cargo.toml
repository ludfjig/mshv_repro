[package]
name = "guest"
version = "0.1.0"
edition = "2021"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
compiler_builtins = {version = "0.1.113", features = ["no-f16-f128", "mem"]}